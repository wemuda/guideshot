---
'@guideshot/core': minor
'@guideshot/cli': minor
---

Add bounded concurrent capture with configurable workers and exclusive scenario keys. Report the CLI version from package metadata so releases cannot drift from `guideshot --version`.
